spring:
  mvc:
    problemdetails:
      enable: true

  datasource:
    url: jdbc:mysql://localhost:12500/Library_Management_System
    username: root
    password: Ijse123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

servlet:
  multipart:
    enabled: true
    max-file-size: 5MB
    max-request-size: 10MB

server:
  port: 8080
  forward-headers-strategy: framework

app:
  storage:
    path: ${user.home}/.ijse.eca.users